# =============================================================================
# Retroactive manifest for e4_baselines
# Generated by hft-ops/scripts/generate_retroactive_manifests.py
# =============================================================================
# This manifest is METADATA-ONLY — all stages are disabled. Its purpose is to
# populate the ledger with a record pointing at pre-existing artifacts under
# lob-model-trainer/outputs/experiments/e4_baselines/
# so `hft-ops compare` can surface historical results alongside new runs.
#
# Usage:
#   hft-ops ledger backfill experiments/retroactive/e4_baselines.yaml \
#       --metrics-file ../lob-model-trainer/outputs/experiments/e4_baselines/training_history.json \
#       --record-type <type>
# =============================================================================

experiment:
  name: e4_baselines
  description: Retroactive sweep_aggregate record for 'e4_baselines'. Artifacts under
    lob-model-trainer/outputs/experiments/e4_baselines/.
  hypothesis: See EXPERIMENT_INDEX.md for the original hypothesis of e4_baselines.
  contract_version: '2.2'
  tags:
  - retroactive-sweep_aggregate
pipeline_root: ..
stages:
  extraction:
    enabled: false
  raw_analysis:
    enabled: false
  dataset_analysis:
    enabled: false
  training:
    enabled: false
  signal_export:
    enabled: false
  backtesting:
    enabled: false
