# =============================================================================
# Retroactive manifest for hybrid_readability_ridge_h10
# Generated by hft-ops/scripts/generate_retroactive_manifests.py
# =============================================================================
# This manifest is METADATA-ONLY — all stages are disabled. Its purpose is to
# populate the ledger with a record pointing at pre-existing artifacts under
# lob-model-trainer/outputs/experiments/hybrid_readability_ridge_h10/
# so `hft-ops compare` can surface historical results alongside new runs.
#
# Usage:
#   hft-ops ledger backfill experiments/retroactive/hybrid_readability_ridge_h10.yaml \
#       --metrics-file ../lob-model-trainer/outputs/experiments/hybrid_readability_ridge_h10/training_history.json \
#       --record-type <type>
# =============================================================================

experiment:
  name: hybrid_readability_ridge_h10
  description: Retroactive calibration record for 'hybrid_readability_ridge_h10'.
    Artifacts under lob-model-trainer/outputs/experiments/hybrid_readability_ridge_h10/.
  hypothesis: See EXPERIMENT_INDEX.md for the original hypothesis of hybrid_readability_ridge_h10.
  contract_version: '2.2'
  tags:
  - retroactive-calibration
pipeline_root: ..
stages:
  validation:
    # 2026-08-15: this manifest is METADATA-ONLY -- every other stage is
    # already disabled. `validation` was the one stage nobody wrote down, so
    # it defaulted to enabled=true and contradicted the header above. Stating
    # it makes the "all stages disabled" claim true.
    enabled: false

  extraction:
    enabled: false
  raw_analysis:
    enabled: false
  dataset_analysis:
    enabled: false
  training:
    enabled: false
  signal_export:
    enabled: false
  backtesting:
    enabled: false
