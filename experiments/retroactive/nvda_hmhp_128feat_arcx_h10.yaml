# =============================================================================
# Retroactive manifest for nvda_hmhp_128feat_arcx_h10
# Generated by hft-ops/scripts/generate_retroactive_manifests.py
# =============================================================================
# This manifest is METADATA-ONLY — all stages are disabled. Its purpose is to
# populate the ledger with a record pointing at pre-existing artifacts under
# lob-model-trainer/outputs/experiments/nvda_hmhp_128feat_arcx_h10/
# so `hft-ops compare` can surface historical results alongside new runs.
#
# Usage:
#   hft-ops ledger backfill experiments/retroactive/nvda_hmhp_128feat_arcx_h10.yaml \
#       --metrics-file ../lob-model-trainer/outputs/experiments/nvda_hmhp_128feat_arcx_h10/training_history.json \
#       --record-type <type>
# =============================================================================

experiment:
  name: nvda_hmhp_128feat_arcx_h10
  description: 'HMHP on 233-day XNAS 128-feature export (profiler-aligned).

    Primary focus on H10, with H60/H300 for multi-horizon context.'
  hypothesis: See EXPERIMENT_INDEX.md for the original hypothesis of nvda_hmhp_128feat_arcx_h10.
  contract_version: '2.2'
  tags:
  - retroactive-training
  - nvda
  - arcx
  - hmhp
  - 128-features
  - mlofi
  - multi-horizon
  - h10-primary
  - profiler-aligned
pipeline_root: ..
stages:
  extraction:
    enabled: false
  raw_analysis:
    enabled: false
  dataset_analysis:
    enabled: false
  training:
    enabled: false
    config: lob-model-trainer/configs/experiments/nvda_hmhp_128feat_arcx_h10.yaml
  signal_export:
    enabled: false
  backtesting:
    enabled: false
