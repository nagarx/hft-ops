# =============================================================================
# Retroactive manifest for e5_60s_huber_nocvml
# Generated by hft-ops/scripts/generate_retroactive_manifests.py
# =============================================================================
# This manifest is METADATA-ONLY — all stages are disabled. Its purpose is to
# populate the ledger with a record pointing at pre-existing artifacts under
# lob-model-trainer/outputs/experiments/e5_60s_huber_nocvml/
# so `hft-ops compare` can surface historical results alongside new runs.
#
# Usage:
#   hft-ops ledger backfill experiments/retroactive/e5_60s_huber_nocvml.yaml \
#       --metrics-file ../lob-model-trainer/outputs/experiments/e5_60s_huber_nocvml/training_history.json \
#       --record-type <type>
# =============================================================================

experiment:
  name: e5_60s_huber_nocvml
  description: 'E5 Phase 2 baseline: TLOB regression on 60s time-based data at H10
    (10 min).

    Establishes deep learning baseline on highest-IC bin (0.248).

    Must beat TemporalRidge IC=0.306 baseline.'
  hypothesis: See EXPERIMENT_INDEX.md for the original hypothesis of e5_60s_huber_nocvml.
  contract_version: '2.2'
  tags:
  - retroactive-training
  - e5
  - nvda
  - regression
  - h10
  - 60s
  - huber
  - no-cvml
  - baseline
pipeline_root: ..
stages:
  extraction:
    enabled: false
  raw_analysis:
    enabled: false
  dataset_analysis:
    enabled: false
  training:
    enabled: false
    config: lob-model-trainer/configs/experiments/e5_60s_huber_nocvml.yaml
  signal_export:
    enabled: false
  backtesting:
    enabled: false
