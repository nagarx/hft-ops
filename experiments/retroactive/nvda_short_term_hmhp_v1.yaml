# =============================================================================
# Retroactive manifest for nvda_short_term_hmhp_v1
# Generated by hft-ops/scripts/generate_retroactive_manifests.py
# =============================================================================
# This manifest is METADATA-ONLY — all stages are disabled. Its purpose is to
# populate the ledger with a record pointing at pre-existing artifacts under
# lob-model-trainer/outputs/experiments/nvda_short_term_hmhp_v1/
# so `hft-ops compare` can surface historical results alongside new runs.
#
# Usage:
#   hft-ops ledger backfill experiments/retroactive/nvda_short_term_hmhp_v1.yaml \
#       --metrics-file ../lob-model-trainer/outputs/experiments/nvda_short_term_hmhp_v1/training_history.json \
#       --record-type <type>
# =============================================================================

experiment:
  name: nvda_short_term_hmhp_v1
  description: 'Short-term focused HMHP with H10/H20 horizons and 40 evidence-based
    features.

    Designed for intraday options trading with emphasis on quick directional predictions.

    Evidence-based feature selection from correlation analysis.'
  hypothesis: See EXPERIMENT_INDEX.md for the original hypothesis of nvda_short_term_hmhp_v1.
  contract_version: '2.2'
  tags:
  - retroactive-training
  - nvda
  - hmhp
  - short-term
  - h10-h20
  - feature-selection
  - options-trading
  - intraday
  - experiment
pipeline_root: ..
stages:
  extraction:
    enabled: false
  raw_analysis:
    enabled: false
  dataset_analysis:
    enabled: false
  training:
    enabled: false
    config: lob-model-trainer/configs/experiments/nvda_short_term_hmhp_v1.yaml
  signal_export:
    enabled: false
  backtesting:
    enabled: false
