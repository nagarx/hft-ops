# =============================================================================
# Retroactive manifest for e6_calibrated_conviction
# Generated by hft-ops/scripts/generate_retroactive_manifests.py
# =============================================================================
# This manifest is METADATA-ONLY — all stages are disabled. Its purpose is to
# populate the ledger with a record pointing at pre-existing artifacts under
# lob-model-trainer/outputs/experiments/e6_calibrated_conviction/
# so `hft-ops compare` can surface historical results alongside new runs.
#
# Usage:
#   hft-ops ledger backfill experiments/retroactive/e6_calibrated_conviction.yaml \
#       --metrics-file ../lob-model-trainer/outputs/experiments/e6_calibrated_conviction/training_history.json \
#       --record-type <type>
# =============================================================================

experiment:
  name: e6_calibrated_conviction
  description: 'E6: Prediction calibration (variance matching) + high-conviction filtering.

    Uses E5 best model checkpoint. No retraining.'
  hypothesis: See EXPERIMENT_INDEX.md for the original hypothesis of e6_calibrated_conviction.
  contract_version: '2.2'
  tags:
  - retroactive-calibration
  - e6
  - nvda
  - regression
  - h10
  - 60s
  - calibrated
  - conviction
  - deep-itm
pipeline_root: ..
stages:
  extraction:
    enabled: false
  raw_analysis:
    enabled: false
  dataset_analysis:
    enabled: false
  training:
    enabled: false
    config: lob-model-trainer/configs/experiments/e6_calibrated_conviction.yaml
  signal_export:
    enabled: false
  backtesting:
    enabled: false
