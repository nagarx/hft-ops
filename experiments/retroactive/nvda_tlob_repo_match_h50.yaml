# =============================================================================
# Retroactive manifest for nvda_tlob_repo_match_h50
# Generated by hft-ops/scripts/generate_retroactive_manifests.py
# =============================================================================
# This manifest is METADATA-ONLY — all stages are disabled. Its purpose is to
# populate the ledger with a record pointing at pre-existing artifacts under
# lob-model-trainer/outputs/experiments/nvda_tlob_repo_match_h50/
# so `hft-ops compare` can surface historical results alongside new runs.
#
# Usage:
#   hft-ops ledger backfill experiments/retroactive/nvda_tlob_repo_match_h50.yaml \
#       --metrics-file ../lob-model-trainer/outputs/experiments/nvda_tlob_repo_match_h50/training_history.json \
#       --record-type <type>
# =============================================================================

experiment:
  name: nvda_tlob_repo_match_h50
  description: 'TLOB model with EXACT preprocessing matching official repository.

    Global Z-score normalization (all prices together, all sizes together).

    BiN layer for adaptive per-sample normalization on top.

    This is the CORRECT approach per TLOB paper and repo.'
  hypothesis: See EXPERIMENT_INDEX.md for the original hypothesis of nvda_tlob_repo_match_h50.
  contract_version: '2.2'
  tags:
  - retroactive-training
  - nvda
  - tlob
  - horizon-50
  - tlob-repo-match
  - global-zscore
  - 40-features
  - official-preprocessing
pipeline_root: ..
stages:
  extraction:
    enabled: false
  raw_analysis:
    enabled: false
  dataset_analysis:
    enabled: false
  training:
    enabled: false
    config: lob-model-trainer/configs/experiments/nvda_tlob_repo_match_h50.yaml
  signal_export:
    enabled: false
  backtesting:
    enabled: false
