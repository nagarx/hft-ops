# =============================================================================
# Retroactive manifest for simple_model_ablation
# Generated by hft-ops/scripts/generate_retroactive_manifests.py
# =============================================================================
# This manifest is METADATA-ONLY — all stages are disabled. Its purpose is to
# populate the ledger with a record pointing at pre-existing artifacts under
# lob-model-trainer/outputs/experiments/simple_model_ablation/
# so `hft-ops compare` can surface historical results alongside new runs.
#
# Usage:
#   hft-ops ledger backfill experiments/retroactive/simple_model_ablation.yaml \
#       --metrics-file ../lob-model-trainer/outputs/experiments/simple_model_ablation/training_history.json \
#       --record-type <type>
# =============================================================================

experiment:
  name: simple_model_ablation
  description: Retroactive sweep_aggregate record for 'simple_model_ablation'. Artifacts
    under lob-model-trainer/outputs/experiments/simple_model_ablation/.
  hypothesis: See EXPERIMENT_INDEX.md for the original hypothesis of simple_model_ablation.
  contract_version: '2.2'
  tags:
  - retroactive-sweep_aggregate
pipeline_root: ..
stages:
  extraction:
    enabled: false
  raw_analysis:
    enabled: false
  dataset_analysis:
    enabled: false
  training:
    enabled: false
  signal_export:
    enabled: false
  backtesting:
    enabled: false
