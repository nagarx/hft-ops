# =============================================================================
# Retroactive manifest for nvda_hmhp_40feat_h10
# Generated by hft-ops/scripts/generate_retroactive_manifests.py
# =============================================================================
# This manifest is METADATA-ONLY — all stages are disabled. Its purpose is to
# populate the ledger with a record pointing at pre-existing artifacts under
# lob-model-trainer/outputs/experiments/nvda_hmhp_40feat_h10/
# so `hft-ops compare` can surface historical results alongside new runs.
#
# Usage:
#   hft-ops ledger backfill experiments/retroactive/nvda_hmhp_40feat_h10.yaml \
#       --metrics-file ../lob-model-trainer/outputs/experiments/nvda_hmhp_40feat_h10/training_history.json \
#       --record-type <type>
# =============================================================================

experiment:
  name: nvda_hmhp_40feat_h10
  description: 'HMHP with short_term_40 preset (40 evidence-based features) on XNAS
    128-feat export.

    Feature selection test: does fewer features = less noise and comparable accuracy?'
  hypothesis: See EXPERIMENT_INDEX.md for the original hypothesis of nvda_hmhp_40feat_h10.
  contract_version: '2.2'
  tags:
  - retroactive-training
  - nvda
  - xnas
  - hmhp
  - 40-features
  - feature-selection
  - short-term-preset
pipeline_root: ..
stages:
  extraction:
    enabled: false
  raw_analysis:
    enabled: false
  dataset_analysis:
    enabled: false
  training:
    enabled: false
    config: lob-model-trainer/configs/experiments/nvda_hmhp_40feat_h10.yaml
  signal_export:
    enabled: false
  backtesting:
    enabled: false
