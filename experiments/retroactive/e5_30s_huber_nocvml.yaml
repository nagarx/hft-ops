# =============================================================================
# Retroactive manifest for e5_30s_huber_nocvml
# Generated by hft-ops/scripts/generate_retroactive_manifests.py
# =============================================================================
# This manifest is METADATA-ONLY — all stages are disabled. Its purpose is to
# populate the ledger with a record pointing at pre-existing artifacts under
# lob-model-trainer/outputs/experiments/e5_30s_huber_nocvml/
# so `hft-ops compare` can surface historical results alongside new runs.
#
# Usage:
#   hft-ops ledger backfill experiments/retroactive/e5_30s_huber_nocvml.yaml \
#       --metrics-file ../lob-model-trainer/outputs/experiments/e5_30s_huber_nocvml/training_history.json \
#       --record-type <type>
# =============================================================================

experiment:
  name: e5_30s_huber_nocvml
  description: 'E5 Phase 2 data-quantity test: TLOB on 30s bins at H10 (5 min), no
    CVML.

    Tests whether 4x more data compensates for slightly lower IC (0.240 vs 0.248).'
  hypothesis: See EXPERIMENT_INDEX.md for the original hypothesis of e5_30s_huber_nocvml.
  contract_version: '2.2'
  tags:
  - retroactive-training
  - e5
  - nvda
  - regression
  - h10
  - 30s
  - huber
  - no-cvml
  - data-quantity
pipeline_root: ..
stages:
  extraction:
    enabled: false
  raw_analysis:
    enabled: false
  dataset_analysis:
    enabled: false
  training:
    enabled: false
    config: lob-model-trainer/configs/experiments/e5_30s_huber_nocvml.yaml
  signal_export:
    enabled: false
  backtesting:
    enabled: false
