# =============================================================================
# Retroactive manifest for e5_60s_huber_cvml
# Generated by hft-ops/scripts/generate_retroactive_manifests.py
# =============================================================================
# This manifest is METADATA-ONLY — all stages are disabled. Its purpose is to
# populate the ledger with a record pointing at pre-existing artifacts under
# lob-model-trainer/outputs/experiments/e5_60s_huber_cvml/
# so `hft-ops compare` can surface historical results alongside new runs.
#
# Usage:
#   hft-ops ledger backfill experiments/retroactive/e5_60s_huber_cvml.yaml \
#       --metrics-file ../lob-model-trainer/outputs/experiments/e5_60s_huber_cvml/training_history.json \
#       --record-type <type>
# =============================================================================

experiment:
  name: e5_60s_huber_cvml
  description: 'E5 Phase 2 CVML test: TLOB + CVML on 60s data at H10 (10 min).

    Tests if CVML feature mixing improves IC/DA over no-CVML baseline.'
  hypothesis: See EXPERIMENT_INDEX.md for the original hypothesis of e5_60s_huber_cvml.
  contract_version: '2.2'
  tags:
  - retroactive-training
  - e5
  - nvda
  - regression
  - h10
  - 60s
  - huber
  - cvml
pipeline_root: ..
stages:
  extraction:
    enabled: false
  raw_analysis:
    enabled: false
  dataset_analysis:
    enabled: false
  training:
    enabled: false
    config: lob-model-trainer/configs/experiments/e5_60s_huber_cvml.yaml
  signal_export:
    enabled: false
  backtesting:
    enabled: false
