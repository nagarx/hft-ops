# =============================================================================
# Retroactive manifest for nvda_hmhp_triple_barrier_calibrated
# Generated by hft-ops/scripts/generate_retroactive_manifests.py
# =============================================================================
# This manifest is METADATA-ONLY — all stages are disabled. Its purpose is to
# populate the ledger with a record pointing at pre-existing artifacts under
# lob-model-trainer/outputs/experiments/nvda_hmhp_triple_barrier_calibrated/
# so `hft-ops compare` can surface historical results alongside new runs.
#
# Usage:
#   hft-ops ledger backfill experiments/retroactive/nvda_hmhp_triple_barrier_calibrated.yaml \
#       --metrics-file ../lob-model-trainer/outputs/experiments/nvda_hmhp_triple_barrier_calibrated/training_history.json \
#       --record-type <type>
# =============================================================================

experiment:
  name: nvda_hmhp_triple_barrier_calibrated
  description: "HMHP multi-horizon model with CALIBRATED per-horizon Triple Barrier\
    \ thresholds.\nBarriers calibrated to achieve balanced class distribution (~25%\
    \ PT, ~25% SL, ~50% TO).\nCascading state from H50 \u2192 H100 \u2192 H200 with\
    \ cross-horizon confirmation."
  hypothesis: See EXPERIMENT_INDEX.md for the original hypothesis of nvda_hmhp_triple_barrier_calibrated.
  contract_version: '2.2'
  tags:
  - retroactive-training
  - nvda
  - hmhp
  - triple-barrier
  - calibrated
  - per-horizon-barriers
  - balanced-classes
  - multi-horizon
  - cascading
  - focal-loss
pipeline_root: ..
stages:
  extraction:
    enabled: false
  raw_analysis:
    enabled: false
  dataset_analysis:
    enabled: false
  training:
    enabled: false
    config: lob-model-trainer/configs/experiments/nvda_hmhp_triple_barrier_calibrated.yaml
  signal_export:
    enabled: false
  backtesting:
    enabled: false
