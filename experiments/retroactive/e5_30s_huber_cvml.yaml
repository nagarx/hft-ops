# =============================================================================
# Retroactive manifest for e5_30s_huber_cvml
# Generated by hft-ops/scripts/generate_retroactive_manifests.py
# =============================================================================
# This manifest is METADATA-ONLY — all stages are disabled. Its purpose is to
# populate the ledger with a record pointing at pre-existing artifacts under
# lob-model-trainer/outputs/experiments/e5_30s_huber_cvml/
# so `hft-ops compare` can surface historical results alongside new runs.
#
# Usage:
#   hft-ops ledger backfill experiments/retroactive/e5_30s_huber_cvml.yaml \
#       --metrics-file ../lob-model-trainer/outputs/experiments/e5_30s_huber_cvml/training_history.json \
#       --record-type <type>
# =============================================================================

experiment:
  name: e5_30s_huber_cvml
  description: 'E5 Phase 2 best-combo test: TLOB + CVML on 30s bins at H10 (5 min).

    Combines 4x data (vs 60s) with CVML cross-feature mixing.'
  hypothesis: See EXPERIMENT_INDEX.md for the original hypothesis of e5_30s_huber_cvml.
  contract_version: '2.2'
  tags:
  - retroactive-training
  - e5
  - nvda
  - regression
  - h10
  - 30s
  - huber
  - cvml
  - best-combo
pipeline_root: ..
stages:
  extraction:
    enabled: false
  raw_analysis:
    enabled: false
  dataset_analysis:
    enabled: false
  training:
    enabled: false
    config: lob-model-trainer/configs/experiments/e5_30s_huber_cvml.yaml
  signal_export:
    enabled: false
  backtesting:
    enabled: false
