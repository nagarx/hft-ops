# =============================================================================
# Retroactive manifest for nvda_hmhp_regression_h10_primary
# Generated by hft-ops/scripts/generate_retroactive_manifests.py
# =============================================================================
# This manifest is METADATA-ONLY — all stages are disabled. Its purpose is to
# populate the ledger with a record pointing at pre-existing artifacts under
# lob-model-trainer/outputs/experiments/nvda_hmhp_regression_h10_primary/
# so `hft-ops compare` can surface historical results alongside new runs.
#
# Usage:
#   hft-ops ledger backfill experiments/retroactive/nvda_hmhp_regression_h10_primary.yaml \
#       --metrics-file ../lob-model-trainer/outputs/experiments/nvda_hmhp_regression_h10_primary/training_history.json \
#       --record-type <type>
# =============================================================================

experiment:
  name: nvda_hmhp_regression_h10_primary
  description: 'Multi-horizon regression: HMHP-R predicts continuous bps returns at
    H10/H60/H300.

    H10-primary weighting per signal strength analysis.'
  hypothesis: See EXPERIMENT_INDEX.md for the original hypothesis of nvda_hmhp_regression_h10_primary.
  contract_version: '2.2'
  tags:
  - retroactive-training
  - nvda
  - xnas
  - hmhp-regression
  - multi-horizon
  - 128-features
  - h10-primary
  - huber-loss
pipeline_root: ..
stages:
  extraction:
    enabled: false
  raw_analysis:
    enabled: false
  dataset_analysis:
    enabled: false
  training:
    enabled: false
    config: lob-model-trainer/configs/experiments/nvda_hmhp_regression_h10_primary.yaml
  signal_export:
    enabled: false
  backtesting:
    enabled: false
