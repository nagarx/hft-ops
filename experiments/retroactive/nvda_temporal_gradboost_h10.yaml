# =============================================================================
# Retroactive manifest for nvda_temporal_gradboost_h10
# Generated by hft-ops/scripts/generate_retroactive_manifests.py
# =============================================================================
# This manifest is METADATA-ONLY — all stages are disabled. Its purpose is to
# populate the ledger with a record pointing at pre-existing artifacts under
# lob-model-trainer/outputs/experiments/nvda_temporal_gradboost_h10/
# so `hft-ops compare` can surface historical results alongside new runs.
#
# Usage:
#   hft-ops ledger backfill experiments/retroactive/nvda_temporal_gradboost_h10.yaml \
#       --metrics-file ../lob-model-trainer/outputs/experiments/nvda_temporal_gradboost_h10/training_history.json \
#       --record-type <type>
# =============================================================================

experiment:
  name: nvda_temporal_gradboost_h10
  description: GradientBoosting on 53 temporal features. Nonlinear alternative to
    Ridge.
  hypothesis: See EXPERIMENT_INDEX.md for the original hypothesis of nvda_temporal_gradboost_h10.
  contract_version: '2.2'
  tags:
  - retroactive-training
  - nvda
  - xnas
  - temporal-gradboost
  - regression
  - 53-features
  - h10
  - simple-model
pipeline_root: ..
stages:
  extraction:
    enabled: false
  raw_analysis:
    enabled: false
  dataset_analysis:
    enabled: false
  training:
    enabled: false
    config: lob-model-trainer/configs/experiments/nvda_temporal_gradboost_h10.yaml
  signal_export:
    enabled: false
  backtesting:
    enabled: false
